<% if @course.errors.any? %>
	$('#error').html("<%= escape_javascript render('errors')%>");
<% else %>
	$('#error').hide();
	$('#courseList').append("<%= escape_javascript render('course') %>");
	$('#new_course')[0].reset();
<% end %>